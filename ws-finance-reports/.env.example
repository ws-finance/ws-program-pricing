# Copy this file to .env and set the values for your local environment.
# NOTE: do NOT commit real secrets. Use this file only as a template.
CLIENT_ID=your_client_id_here
CLIENT_SECRET=your_client_secret_here
REDIRECT_URI=https://your-public-domain.example.com/callback
# Optional: if you know the company (realmId) in advance you can set it, otherwise authorize via /auth
COMPANY_ID=your_company_realmId_here
# 'sandbox' or 'production'
ENVIRONMENT=production
PORT=3000