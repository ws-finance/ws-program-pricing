{
  "name": "qbo-proxy-server",
  "version": "0.1.0",
  "description": "Simple QuickBooks Online proxy to fetch reports (OAuth2 + token refresh)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "node-fetch": "^2.6.7"
  },
  "engines": {
    "node": ">=16"
  }
}
